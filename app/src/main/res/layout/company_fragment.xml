<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:weightSum="10"
            android:orientation="horizontal">
            <EditText
                android:id="@+id/txtCompanyTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="9"
                android:inputType="text"
                android:hint="@string/sys_title"
                android:autofillHints="@string/sys_title"
                tools:targetApi="o" />

            <ImageButton
                android:id="@+id/cmdCompanySearch"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:contentDescription="@string/sys_search"
                app:srcCompat="@drawable/ic_search_black_24dp"/>
        </LinearLayout>

        <EditText
            android:id="@+id/txtCompanyFoundation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="date"
            android:hint="@string/media_companies_foundation"
            android:autofillHints="@string/media_companies_foundation"
            tools:targetApi="o" />

        <EditText
            android:id="@+id/txtCompanyDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textMultiLine"
            android:hint="@string/sys_description"
            android:autofillHints="@string/sys_description"
            tools:targetApi="o" />
    </LinearLayout>
</ScrollView>